<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.deves.tsaapplication">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="A+ Autograder"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DisplayScores"></activity>
        <activity android:name=".PreviousTests" />
        <activity android:name=".Login" />
        <activity
            android:name=".MainActivity"
            android:label="A+ Autograder">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="com.google.android.gms.vision.DEPEENDENCIES"
                android:value="orc" />
        </activity>
        <activity android:name=".DisplayActivity" />
        <activity android:name=".StudentActivity" />
        <activity android:name=".UserInput" />
    </application>

</manifest>